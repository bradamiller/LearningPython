# Netlify configuration for the XRP Python Curriculum site.
#
# This repo IS the site, so there is no base directory to set — connect the
# repo in Netlify and it reads the settings below.
# (Publish path is relative to the repo root.)

[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
