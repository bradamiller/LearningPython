---
title: "Lesson 1-5 · The Polygon Function"
sidebar_label: "1-5 · Polygon Function"
sidebar_position: 5
---

<LessonHeader
  eyebrow="Module 1 · Learning to Drive"
  title="Lesson 1-5 · The Polygon Function"
  meta={['50–60 min', 'Blockly Foundation', 'Synthesis challenge']}
/>

<TeacherBanner>
Teacher mode is on. Toggle it off (bottom-right) to preview the student view.
</TeacherBanner>

This is the big one. You'll combine everything from Lessons 1-2 through 1-4 — loops,
functions, parameters, and the angle formula — into a single function that draws
**any** regular polygon. Tell it how many sides and how long, and it figures out
the rest.

<Objectives>

By the end of this lesson you will be able to:

- Combine a loop, a function, and parameters to solve one bigger problem
- Compute the turn angle *inside* the function with `360 ÷ sides`
- Test one function against many shapes (triangle, square, hexagon, octagon…)
- Explain how one general function replaces a whole pile of specific ones

</Objectives>

<QuizLink id="module-01-driving/lesson-05-polygon-function" />

## One function to draw them all

So far each shape needed its own function. But look at the pattern: every polygon
is *repeat N times: drive a side, turn `360 ÷ N`*. The only things that change are
the number of sides and the side length — perfect for **parameters**. And the
angle? The function can calculate it itself.

<BlockShot
  src="/img/blocks/programs/polygon-function.png"
  alt="A polygon function with parameters sides and side_length: a Repeat sides times containing Straight cm side_length and Turn Deg 360 ÷ sides"
  maxWidth={520}
  caption="One function draws every regular polygon. The two parameters are sides and side_length; the turn angle is computed from sides."
/>

The new trick is putting a **math division block** (`360 ÷ sides`) right into the
Turn block's angle slot. Now `sides` does double duty: it sets how many times the
loop repeats *and* it sets the angle.

Notice what is *not* a parameter: **Effort** stays at `0.5` in both blocks. A
parameter earns its place when callers actually need to vary it — `sides` and
`side_length` change with every shape, speed doesn't.

<TeacherNote title="Building the division">

The one unfamiliar step is the Math → division block. Have students drag `360 ÷ [ ]`
into the Turn angle slot, then drop the `sides` variable into the blank. Point out
that `sides` is used in two places — the repeat count and the angle — which is why
this single function generalizes. Build the square test case first and confirm it
works before turning students loose on other shapes.

</TeacherNote>

## Activity · Test it like an engineer

Real programmers test one function against many inputs. In your main program, call
`polygon` with different values and predict each shape before it runs:

```text
Wait for button press
call polygon (sides: 4, side_length: 30)
call polygon (sides: 3, side_length: 30)
call polygon (sides: 6, side_length: 20)
call polygon (sides: 8, side_length: 15)
```

Write down what you expect each line to draw **before** you upload. Then run it
and see whether the robot agrees with you.

<Reveal title="Check your predictions" hint="write yours down first">

Square, triangle, hexagon, octagon — in that order. The side lengths shrink as the
shapes gain sides, because a shape with more sides needs more floor for the same
side length.

</Reveal>

<KnowledgeCheck
  question="What turn angle will polygon compute for a 12-sided shape (a dodecagon)?"
  options={[
    {text: '12°'},
    {text: '30°', correct: true},
    {text: '36°'},
    {text: '90°'},
  ]}
  explanation="The function calculates 360 ÷ 12 = 30° on its own — no need to work it out by hand."
/>

<KnowledgeCheck
  question="polygon (8, 25) traces an octagon about 2 m around. What does polygon (8, 50) do?"
  options={[
    {text: 'A different shape'},
    {text: 'The same octagon shape, but about twice as big (~4 m around)', correct: true},
    {text: 'An octagon at double speed'},
    {text: 'Nothing changes'},
  ]}
  explanation="Doubling side_length doubles each side, so the perimeter doubles — but the angles (and therefore the shape) stay the same."
/>

## Activity · Explore

1. Run the four test shapes above and check each one roughly closes back to its
   start.
2. Notice what happens as sides go up (10, 12, 20): the shape gets closer and
   closer to a **circle**.
3. **Challenge:** draw a pentagon, `Turn 72°`, then draw the pentagon again — the
   two overlapping pentagons make a star/mandala.

<KnowledgeCheck
  question="Without a general polygon function, you'd write one function each for square, triangle, pentagon, hexagon, and octagon. With polygon, how many functions do you need?"
  options={[
    {text: 'Five'},
    {text: 'One', correct: true},
    {text: 'One per shape you draw'},
    {text: 'Three'},
  ]}
  explanation="That's the power of generalizing: a single well-designed function replaces all the special-case ones."
/>

<TeacherNote title="Differentiation & debugging">

**More support:** give a skeleton with the Repeat block already placed and only
ask students to add Straight and the `360 ÷ sides` Turn. **More challenge:** a
"spiral polygon" that grows each side, or a `star` helper built on top of
`polygon`. **When a shape looks off:** first confirm the math (`360 ÷ 6 = 60`),
then rule out wheel slip by testing at effort `0.5` — many "bugs" here are physics,
not code.

</TeacherNote>

## Real-world connections

Writing one flexible tool instead of many rigid ones is the heart of good engineering:

<CardGrid>
  <InfoCard tag="Software" title="Libraries">
    A charting library draws any chart from parameters — you don't write new code for every graph.
  </InfoCard>
  <InfoCard tag="Design" title="Parametric CAD">
    Engineers define a part once with parameters, then generate every size from the same model.
  </InfoCard>
  <InfoCard tag="Manufacturing" title="Configurable machines">
    One programmable machine makes many products by changing inputs, not by rebuilding the machine.
  </InfoCard>
</CardGrid>

## Wrap-up

- What two parameters does `polygon` take, and what does each control?
- How does the function get the turn angle? (`360 ÷ sides`, computed inside.)
- Why is one general function better than many specific ones? (Less code, fewer bugs, easier to change.)

<TeacherNote title="Assessment & next lesson">

**Formative:** can students explain why `sides` appears in two places, and predict
a shape from a call? **Exit ticket:** "Turn angle for a 9-sided polygon?"
(`360 ÷ 9 = 40°`). **Preview:** next lesson we go *under* `straight` and `turn` to
control the left and right motors directly.

</TeacherNote>


## Resources

- [XRP Code IDE](https://xrpcode.wpi.edu/)
- [XRP Robot User Guide](https://xrpusersguide.readthedocs.io/)
